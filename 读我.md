# 盘古

> 自与非门起，自底向上重建一整套以中文与易学为命名本源的计算体系。
> 非为替代 Linux，而为造一台自洽可运行的计算机 + 操作系统 + 工具链。

## 何物

| 名 | 所指 |
|---|---|
| **盘古开天** | 计划之名 |
| **盘古** | 仓库之根（此目录） |
| **爻机** | 二十四位机器 |
| **太极** | 处理器，运化之枢 |
| **演** | 模拟器（L1 门级 / L2 周期 / L3 指令） |
| **枢 / 叩 / 化 / 序** | 内核 / 系统调用 / 进程 / 调度 |

命名总表见 `典/00-命名总表.md`，总纲见 `典/04-开天之图.md`。

## 运行

Go 装在隔离目录 `~/.workbuddy/binaries/go/go`（**未**入系统 PATH，亦未改 `~/.zshrc`）。
仓库根的 `跑.sh` 会自动设好 GOROOT/GOPATH/GOCACHE：

```sh
./跑.sh test ./...          # 全量测试
./跑.sh vet ./...           # 静态检查
./跑.sh run ./cmd/pangu     # 运行演示

穷举=1 ./跑.sh test -count=1 -run Test往返穷举 ./...   # 七百万字之全遍历，约十五秒
zsh 具/变异译.sh                                       # 译卦器之变异测试，十六变异
zsh 具/变异枢.sh                                       # 枢之变异测试，四十五变异
```

后三者**默认不开**：穷举太慢，变异会改源码（虽能自愈，仍宜先提交再跑）。

变异之器**须以 zsh 呼之**，**不可接 `| head`**（曾死于 SIGPIPE，而以带变异之版为原样）；
改哪个文件便先备份哪个——纵自愈失手，还有 `git checkout --`。

若无 `跑.sh`（或想直接用 go），手工设一次环境即可：

```sh
export GOROOT=$HOME/.workbuddy/binaries/go/go
export GOPATH=$HOME/.workbuddy/binaries/go/gopath
export GOCACHE=$HOME/.workbuddy/binaries/go/gocache
export PATH=$GOROOT/bin:$PATH
```

## 源码

单一 Go 包 `package 盘古`，源文件平铺仓库根，分层靠文件名而不靠目录：

| 文件 | 所司 |
|---|---|
| `爻.go` | 阴阳：爻之本体与七种运算 |
| `象.go` | 四象、八卦、六爻之符，与两套位序之转换 |
| `门.go` | **演 L1**：线、门、网表、事件驱动调定 |
| `派生.go` | 万门出于一：仅用与非门派生诸门 |
| `算术.go` | 半加器 → 全加器 → 二十四位算爻器 |
| `时序.go` | 辰、锁爻器、存爻器、卦器堆、藏、计爻器 |
| `卦序.go` | 文王序 ↔ 先天序之映射（M3 前置） |
| `令.go` | 指令集之规范：编解码、符号扩展、反汇编 |
| `太极.go` | **演 L1**：门级中央处理器，三万三千余门 |
| `周期.go` | **演 L2**：周期级模拟器，不算门只算整数；有 `机态` 之较，为对拍之凭 |
| `程.go` | 手编之机器码（斐波那契、累加之属），共用之物；M4 起各附其**源码** |
| `译.go` | **译卦器**：读汉字助记符与卦符，出 24 位机器码；又能反汇编 |
| `枢.go` | **演 L3**：枢机。特权两态、爻变五因、**界址（基址＋限长）**、器区十口、叩之六十四号 |
| `枢程.go` | **枢之源码**：内核以译卦器之语自陈，由 `汇` 译成码，载于藏之高区；**序 亦是码**（二十令） |
| `演示.go` | 命令行演示 |
| `出口.go` | 唯一通向包外的窄门 `Run()` |
| `cmd/pangu/` | 命令行入口（目录名必须 ASCII） |
| `具/` | 非 Go 之辅助脚本（目录名 ASCII，以备他日须被引用） |
| `具/变异译.sh` | 译卦器之变异测试：十六变异，逐一注入，看诸验能否杀之；五道自检以自证可信 |
| `具/变异枢.sh` | 枢之变异测试：四十五变异；**七道自检**，含「残留之检」——备份与标记皆固定名，纵被 SIGKILL 亦能自愈 |

**为何是单包**：汉字在 Unicode 中属 Lo 类而非 Lu 类，按 Go 规范一律不可导出。
故欲「全中文」，便只能是一个包。详见 `典/02-实现约束.md`。

## 典（规范与记录）

| 文档 | 内容 |
|---|---|
| `典/00-命名总表.md` | 中文血统命名总表 |
| `典/01-爻机规格.md` | 二十四位机器规格、指令格式 |
| `典/02-实现约束.md` | **必读**：九条实现约束，含位运算之戒与时序之约 |
| `典/03-里程碑.md` | M0–M9 验收标准与现状 |
| `典/04-开天之图.md` | 计划全文（总纲） |
| `典/05-营造日志.md` | 逐日营造记录：做了什么、撞了什么墙 |
| `典/06-论中文操作系统.md` | 论文：中文操作系统工程之诸阶段 |
| `典/07-卦序映射表.md` | 文王序 ↔ 先天序之全表与核法 |
| `典/08-太极运化.md` | M3 全案：指令集、数据通路、两拍一令、对拍与变异测试 |
| `典/09-译卦器.md` | M4 全案：往返之验、双向之约、语法、变异测试 |
| `典/10-枢与叩.md` | **M5 全案**：特权、爻变、叩之六十四号、器区、字之文、变异之瞎处 |
| `典/11-化与序.md` | **M6 全案**：界址、化块、序之码、钟、告、器区十口、变异之账 |
| `典/12-籍与目.md` | **M7 全案**：籍区之址、一节之形、籍之六叩、壳之码、变异之账 |

## 进度

- **M0 阴阳与门** —— 已成（L1 门级模拟器，16 项测试全绿）
- **M1 四象算术** —— 已成（二十四位算爻器，2472 门；八操以一卦择之）
- **M2 八卦时序** —— 已成（锁爻器 → 卦器堆 → 藏 → 辰 → 计爻器；时序既启，方得称「机」）
- **M3 太极运化** —— 已成（数据通路 + 控制单元 + ISA；L1 门级三万三千余门，
  L2 周期级快十九万倍；**两机逐拍对拍，十三案 324 拍处处相合**）
- **M4 译卦器** —— **已成**（汇编器与反汇编器；人写汉字助记符与卦符，机器出 24 位码；
  **往返穷举 7 026 365 字无一不合**；与手编之码**逐字相合**；**十六变异，十六皆杀**）
- **M5 枢与叩** —— **已成**（L3 指令级；特权两态、爻变三因、器区八口、叩之六十四号；
  **枢自己是码**——以译卦器之语写就，由 `汇` 译成码载于高区，Go 里没有一行内核逻辑。
  **L2↔L3 逐拍对拍 884 拍处处相合**；**廿五变异，廿五皆杀**）
- **M6 化与序** —— **已成**（**界址＝基址＋限长**：物址 ＝ 界基 ＋ 虚址，
  虚址 越界限则陷。化块 一化一底，**底在块，深在栈**；
  **序 亦是码**——二十条令，Go 那一侧没有一行调度逻辑；
  钟 之片以**令**计，告 之变**不耗一令**。
  验收：两化**同用一虚址**而各落一物址，交替运行而**和 各是各的指纹**；
  **四十五变异，四十五皆杀**）
- **M7 籍与目** —— **已成**（**籍区居枢基之下**，化之极 由 籍基 出。
  一节三十二字：**名 以零终而不另记其长**，长 只记 据 之字数；
  节址 ＝ 籍基 ＋（号 << 节之移），**以移位代乘**。
  **籍之六叩以节号为凭，不以节址为凭**——与其验其址，不如不与之址。
  **籍 亦是码**，**壳 亦是一「化」**：六令（列 建 写 读 删 止），
  写 读 删 先 求 其号而后叩。
  验收：一稿自「建」跑到「止」，**九令九报**；
  名**径自藏中读出**，是 U+5973 U+5A32 那两个码位本身。
  **五十六变异，皆杀**）

**M7 之验收**（`典/04` 原文「能创建、读写、列举文件；文件名是 UTF-8 汉字」）：
`Test壳之全程`，九令九报，逐字核之：

```
所入：建 盘古 / 写 盘古 开天 / 读 盘古 / 建 女娲 / 写 女娲 造人
      列 / 删 盘古 / 列 / 止
所出：〉成 〉成 〉开天 〉成 〉成 〉盘古 女娲 〉成 〉女娲 〉
```

「文件名是汉字」不是靠「能存进去」，而是靠**径自藏中读出来**——
不假 叩目 之手（叩目 亦是码，以码验码则译错之处两处同错）。

**M3 之验收**（`典/04` 原文「跑通一段手写的 24 位机器码，L1 与 L2 结果一致」）：
手编之斐波那契十一字，L1 跑 **132 拍得 离 = 55 = fib(10)**；
L2 同跑，拍数与诸器之果**逐拍相合**。

**M4 之验收**（原文「round-trip 测试：汇编 → 机器码 → 反汇编 → 文本一致」）：
`汇一(令之译(令)) ≡ 令`，全域穷举 **7 026 365 字**无一不合
（日常跑前两层共 210 621 字，0.49 秒；全域一层设环境变量 `穷举` 方跑，14.75 秒）。
又与 M3 手编之码逐字相合：斐波那契 11 字、存取 7 字、累加 6 字。
译出之码径付 L2 跑之，**132 拍得 离 = 55**——与手编者一拍不差。

写一段源码给它：

```
；斐波那契
加数 艮, 坤, 10
加数 坎, 坤, 0
加数 巽, 坤, 1
环：等跃 艮, 坤, 毕
加 震, 坎, 巽
加 坎, 巽, 坤
加 巽, 震, 坤
减数 艮, 艮, 1
跃 环
毕：加 离, 坎, 坤
停机
```

**M5 之验收**（原文「用户态执行『叩』、内核接管并打出汉字——第一个操作系统闭环」）：

```
；用户态。化 不识器区，不识枢之域，唯有「叩」一途可达
处 0o100
加数 兑, 坤, 数区     ；兑 ← 欲言之首址  （元一）
加数 离, 坤, 5        ；离 ← 字数        （元二）
加数 乾, 坤, 1        ；乾 ← 叩号 ＝ 言
叩   0               ；求枢言之
加数 乾, 坤, 3        ；乾 ← 叩号 ＝ 息
加数 兑, 坤, 0        ；兑 ← 退出之码
叩   0

处 0o200
数区：
      字 '盘'
      字 '古'
      字 '开'
      字 '天'
      字 '！'
```

跑 **94 拍**，串口出 **「盘古开天！」** 五个汉字。
那五个字不是 Go 打出来的，是**码**打出来的：
化 行 `叩` → 枢 接管 → 查叩之表 → 跃往 枢文 → 逐字写 `出之口` → `回` 复权 → 化 行 `叩 3` 而止。
全程 化 未尝一触 枢之域，未尝一行特权之令——**触了便陷，陷了便撤**。

L3 之可信，两途证之：与 L2 **逐拍对拍 884 拍**（五段码，计/令/相/址/拍/诸器/诸字
逐一位次相合）；**廿五变异，廿五皆杀**。

**M6 之验收**（原文「两个『化』交替运行，各自的内存互不干扰」）：
两个源码**完全相同**的化（皆自虚址 0 起，故**同一段码装在两处**），
一个累加三回出「甲」，一个累加五回出「乙」：

```
片 1：出「甲乙甲乙甲乙乙乙」，告 114 次
片 8：出「甲乙甲乙甲乙乙乙」，告 13 次
片 0：出「甲甲甲乙乙乙乙乙」，告 0 次   ；协作式，一化自息而后他化方行
```

三者之 **计** 与 **和** 分毫不差：甲 3 × 30002 ＝ 90006，乙 5 × 20057 ＝ 100285。

「互不干扰」不是靠两化各用一址——那是理所当然，验不出什么。
此验**故意让两化用同一个虚址** `化和址`，而它们各落在 `0o000140` 与
`0o000240` 两处物址上。**和 ← 和 ＋ 其字**，故 和 是此化之**指纹**：
屏若破，两化争写一址，和 遂成 190291——甲不是，乙也不是。

一化越了界（存取越界、取指越界，两条路各有一验），**死的只是它自己**，
序 转其次而他化续行如故——此是 M5 与 M6 之大别。

全量 **118 项**测试全绿，约 9 秒。

## 演 之三档

| 档 | 名 | 所算 | 一拍 | 可信否 |
|---|---|---|---|---|
| L1 | 太极 | 门，逐门重算至定 | 3.2 ms | 可信——全身无一句「假设」 |
| L2 | 周期 | 整数，一门也无 | 17 ns | **不可自信**——全身都是假设 |
| L3 | 枢 | 不算周期，只管**机器与程序之交界** | 视所托 | 所托者 L2，故随 L2 |

L3 不另立一套指令语义——它把每条令的**式**全数委给 L2 的 `步()`，
自己只加 L2 所无者：**特权、爻变、器区**（M5），**界址、化、序**（M6）。
> 抽线，不抄式。一份式若有两处写，其中一处必朽。

L2 之可信不由其自身证明，而由 L1 一拍一拍地替它证明。
**慢者是尺，快者是布；布之长短，须以尺量之，不可自夸。**

## Git

远端在群晖 NAS（ds923-wbq，经 Tailscale）：

```sh
git push                  # 已设上游，等价于 git push origin main
git pull
# remote origin: ssh://nas-ts/volume1/GitRepo/pangu.git
```

换机器时取库：

```sh
git clone ssh://nas-ts/volume1/GitRepo/pangu.git
git config core.hooksPath 钩      # 启用预提交之钩
git config core.precomposeunicode true   # macOS 上必设：令中文文件名以 NFC 入库
git config core.quotepath false          # 令中文路径正常显示，而非八进制转义
```

**预提交之钩**（`钩/pre-commit`）会拦下四类问题：未经 `gofmt` 的文件、`go vet` 未过、
测试未过、以及非 NFC 规范化的文件名。已随库分发，但 `core.hooksPath` 是本地配置，
须如上手工设一次。

若日后另加 GitHub 镜像，请命名为 `github`，勿动 `origin`（后者指向 NAS）。

## 十四戒

1. **凡位运算与算术混写，括号一律不省**（`&` 与 `<<` 同级而左结合，一日咬过三次）。
2. **两套位序禁止混用**：数值序与卦象序互为镜像，转换必走 `排爻`/`并爻`/`倒爻`。
3. **万门出于一**：一切受入而算之门，皆须由与非门派生；恒源不与焉。
4. **先置入，后拍辰**：欲写某器，须先置其入与存允，调定已安，方可拍辰；
   拍过之后，方可更其入。此即建立时间与保持时间，违之则错而无声。
5. **量具亦须被量**：一个说「一切正常」的检查，必须先证明它**分得出不正常**——
   否则它的沉默什么也不说明。（M4 之训：`典/02` §十）
6. **凡手抄之单，必漏**：一处知识若有两份拷贝，其中一份必朽；
   而朽的那份若是个**过滤器**（名单、白名单、`-run` 之选），
   它漏掉的东西**不报错，只是不见了**——这是最难察的一类错。
7. **瞎处须另立一验**：一个验法验不到的东西，跑一万遍也是验不到——
   它不会报错，它只是一遍遍地绿。
   （`停机` 之址无义，「停机 0」与「停机」回译同字，
   故往返穷举七百万字也杀不着那个变异——于是另立 `Test规范式之全` 验其**形**。）

8. **测试若另立一份枢，则所验者非所跑者。** 枢之诸验一律呼 `建化之机`，
   换的只是 化之程 里那一段**用户程**——机器、枢文、叩之表，皆同一份真物。
   验里若自建一台「宜验之枢」，则所验者是那个赝品，跑的却是真物。
9. **陷阱捕不得 SIGKILL。** `trap … EXIT INT TERM HUP PIPE` 拦得住自尽，
   拦不住他杀（退出码 137 = 128+9）。凡**改人源码**之具，须留**遗书**：
   备份与标记皆用**固定名**（不掺 `$$`，否则新的一趟认不出上一趟的尸首），
   起手见标记即知上趟横死，径自还原。
10. **死于编译之变异，什么也没验到。** 一个令编译器报错的变异，
    只证明「源码改坏了便编不过」，不证明诸验分得出对错。
    故变异之器分记「杀／存活／**可疑**」，可疑者须改式重跑。
    （M5 之训：`典/10` §十一）
11. **一个恒真的断言与没有断言同。** 凡验「某件事**没有**发生」者，
    须先自问「若它发生了，会落在哪一址、哪一数、哪一行」。
    守错了址，则纵一无所有，验也是绿的——它不报错，只是安静地绿着。
    （M6 之训：`Test化态写枢之域则撤` 守在 `枢基` 上，而界址既立，
    越界之写被 `变换` 引去了 `界基`，**根本没落在 枢基 上**。
    那一断言恒真，绿了整整一个里程碑。今以整片 藏 之抄本夹之。）
12. **注里写的机制，须与跑出来的相符。** 一验杀了一个变异，
    不等于它按你以为的那个理杀的。`化之越取源` 初注谓「越址当落在次化之域首，
    那里是能跑的码」，而实测是 `变换` 把它引回了 `界基`、
    化 自取其首令而空转——注与实**相反**。
    注若与实不符，那注便是**下一篇谎**：后人读之，据以改约，而祸在别处。
    故凡志机制于注中者，**先跑一遍，看它究竟是怎么红的**。
13. **与其验其址，不如不与之址。** 一个值若是危险的，
    最省事的屏不是把它验好，是**不把它交出去**。
    籍之六叩以**节号**为凭，不以节址为凭：号 越界者，枢 一较便知；
    而 节址 若落回化态之手，化 遂持一柄枢门的钥匙——
    枢 还得验它是不是真的，而**验得再严，也强不过不给**。
    凡见一处「验得很严」的代码，先问一句：这个东西非给不可吗？（M7 之训）
14. **量具之疆界系于它所书的常数。** 凡验之中有常数，须问：这个数是**因**，还是**果**？
    是果者，改从因出。
    （M7 之训：`Test化态取指于枢之域则越权` 书「跃 0o777000」，
    其时 化之极 ＝ 枢基，故那正是界外之第一字，变异卅五遂死于此；
    及 化之极 移就 籍基，那字便成了界外**很远**之字——远则差一量不出，
    而此验**仍然全绿**：它绿它的，变异活它的。治法：跃向 **化之极**，
    界外第一字之**因**。**一个验杀过一个变异，不等于它永远杀得着。**）

九条实现约束之全帙见 `典/02-实现约束.md`。
各里程碑自有所立之戒：`典/08` §十（M3 之三约）、`典/09` §十三（M4 之三戒）、
`典/10` §十二（M5 之三戒）、`典/11` §十一（M6 之四戒）、`典/12` §九（M7 之三戒）。

又：**汉字不可为 shell 变量之名**（`具/变异枢.sh` 之「汉字之名之检」守之）。
zsh 以 `LC_CTYPE` 判何者是标识符之字符；其地若是 C/POSIX，
则 `验数=141` 不是赋值而是命令，报 `command not found: 验数=141`，
而病根不在那一行，在调用者手里（定时、CI、管道里跑皆是 C 之地）。
脚本内 `export LC_ALL=…UTF-8` **救不得**——解析表在启动之际已定死。
**注里的规矩没人守，规矩照旧会坏**（此具第 36 行早写了这条，其后照样有人写回了汉字名），
故令机器 grep 自身以守之。
