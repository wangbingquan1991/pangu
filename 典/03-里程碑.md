# 里程碑纪要

## M0 · 阴阳与门 —— 已成

**立基之日**：2026-08-30

### 所成之物

| 文件 | 内容 |
|---|---|
| `爻.go` | 爻（bool 之别名）、阴阳二仪、与/或/非/异或/与非/或非/同或 七种基本运算 |
| `象.go` | 四象、八卦、六爻之符；卦象位序与数值位序的转换（`排爻` `并爻` `倒爻`） |
| `门.go` | **演 · 第一档（L1 爻级）**：线、门接口、网表、事件驱动调定算法 |
| `派生.go` | 仅以与非门为原语，派生出九种门与两种多入结构 |
| `演示.go` | 异或真值表、三位译码器以八卦显之 |
| `门_test.go` | 十六项穷举测试 |
| `出口.go` `cmd/pangu/main.go` | 对外唯一非中文入口 |

### 验收（逐条核对）

- [x] **穷举真值表全通过**：与、或、非、与非、或非、异或、同或、择门（3 入 8 组）、
      译门（1–4 位，共 2+8+32+256 组）、八爻奇偶门（256 组）
- [x] **十六爻与门穷举 65 536 组**（`Test十六爻全与门`，0.06 秒）
- [x] **网表可述可求值**：`网表.述()` 印出门表与连线；演示中异或电路 4 门 6 线、
      三位译码器 51 门 54 线
- [x] **万门出于一**：`验纯粹` 断言诸电路落成后，网表中除 `*与非门` 外别无他种原语
- [x] **异或恰由四与非门构成**（`Test异或门` 校验门数为 4）
- [x] **振荡可察**：三个非门首尾成环，`调定` 正确地报出「疑为振荡」而非死循环
- [x] **短路可察**：两门共驱一线，`装门` 立时报错

### 顺带核实的三件事

1. **八卦的 Unicode 排布确为先天序**：U+2630–U+2637 依次为
   乾(HEAVEN) 兑(LAKE) 离(FIRE) 震(THUNDER) 巽(WIND) 坎(WATER) 艮(MOUNTAIN) 坤(EARTH)，
   值与序位满足 `序位 = 7 - 值`。四象同理，`序位 = 3 - 值`。
2. **六十四卦 U+4DC0 段确为文王序**：首为 `HEXAGRAM FOR THE CREATIVE HEAVEN`（乾），
   末为 `BEFORE COMPLETION`（未济）。
3. **Go 中文标识符的三条硬约束**——见 `典/02-实现约束.md`，其中「汉字不可导出」
   一条直接决定了本系统必须是单一包。

### 遗留

- **文王卦序 ↔ 先天二进制映射表**未补。此表是 M3（指令集以六十四卦为操作码）的前置，
  须从权威来源核对，不得凭记忆手抄。在此之前，别卦以「上卦下卦」两经卦表示。

---

## M2 · 八卦时序 —— 已成

**成于**：2026-08-31

> 前此一切皆是组合之电路：入变则出随，无所记忆，过而不留。
> 时序既启，这台机器方才有了「上一刻」与「下一刻」之分——
> **自此可以称「机」**。

### 所成之物

| 文件 | 内容 |
|---|---|
| `时序.go` | 辰、锁爻器、边锁爻器、存爻器、卦器堆、藏、计爻器 |
| `门.go`（增） | `反相`（反相线之簿）、`阴列`、`置数` / `读数`、`总复`（复位之轨） |
| `演示.go`（增） | `演示时序`：锁爻与辰、卦器堆、取指之机 |
| `时序_test.go` | 七项测试 |

**七器之序**，自底而上：

| 器 | 何以 | 门数 |
|---|---|---|
| 锁爻器 | 门控 D 锁存器。两枚与非门交相为入，遂能自持一爻 | 5 |
| 边锁爻器 | 主从上升沿触发器。主以 反辰 为启，从以 辰 为启 | 10 + 共用之反相 |
| 存爻器 | n 位寄存器。存允 择其入，总复 使之归阴 | 位 × 22 |
| 卦器堆 | 八器，以八卦为名；三爻（一卦）择其一；两口同时而读 | 6 693（二十四位） |
| 藏 | 同步而写、随时而读 | 1 813（八字 × 八位） |
| 计爻器 | 可增、可载、可守 | — |
| 辰 | 时序之枢。非电路，乃驱动之约 | — |

### 验收

- [x] **锁爻器**：启阳则透明（出随入），启阴则守（入再变而出不动）
- [x] **边锁爻器**：出唯在上升沿而变。三处要害皆验——
      沿已过而入偷改、沿未来而入偷改，出皆不动
- [x] **存爻器**：存允 制其纳否；总复 一拍归阴
- [x] **卦器堆**（二十四位，6 693 门）：八器各纳一位相异之数（`1 << 3i`），
      逐器读回；两口同择一器所得当同；改写一器而余七器不受扰
- [x] **藏**（八字 × 八位）：逐字写入读回；存允为阴则虽拍而不纳；只改一字而余字不受扰
- [x] **计爻器**：增、载、守三途皆验；八位之计 255 增一当归零
- [x] **机之雏形**：计 驱 藏，逐拍取出 `29j+3` 之序列（3, 32, 61, …, 206）
- [x] 万门出于一：诸电路皆由与非门派生，`验纯粹` 无一所破

### 三处要紧的取舍

**一、无延迟之模拟器，如何建模时钟边沿？**

本模拟器不建传播延迟之模型——一线之负载何时重算，只看事件之序，不看时间。
于是有此一问：无延迟之时序，主从触发器还能正确否？

能。主锁存器以 反辰 为启，从锁存器以 辰 为启。辰 为阴时主透明而纳 入、从守；
辰 转阳时主守其所纳、从纳主之所存。

关键的竞争在于：辰 既转阳，从已开，而 反辰 尚未及变，主犹透明。
此时若有 入 的变化漏过主而达于从，则所见非沿前之值。
但只要**入 在辰翻转之顷稳定不变**，主纵透明，其所存者仍是那个沿前之值。

此即真实电路中的**建立时间与保持时间**。本模拟器把它写成了一条使用之约
（见 `时序.go` 文首「时序之约」），而非电路之律：
先置入、置存允，调定已安，然后拍辰；拍过之后，方可更其入。

**二、何以不用门控时钟？**

写寄存器有两种做法：一是以 存允 去与 辰，各器自生其时钟（门控时钟）；
二是以 存允 择其入，诸器共用一根 辰。

取后者。门控时钟则各器之时钟有别，先后参差（时钟偏移），
快者已纳而慢者未及，一拍之内诸器遂不齐。
纵在本模拟器无延迟之模型中此弊不显，此约亦当立——
**它要防的是将来真机上的偏移，不是今日模拟里的错**。

**三、总复之轨**

锁爻器之初态乃「两出皆阴」的非法之态，收敛到哪一态全凭求值之序。
实测：初始 出 收敛为阳。此与真实电路的上电不定一般无二，
故不能指望「不设复位则自然是零」。

遂立 `总复` 一轨：复为阳时，凡经 `置存爻器` 所造之器，皆于下一有效沿归阴。
之所以用 `所存 &= ¬复`（一个与门）而非再加一重择门，是因为复位只须强制归零，
与门两枚与非门便够，择门却要九枚。

### 遗留

- 藏 之规模甚小（八字 × 八位）。真机之 藏 当以兆计，
  届时须另谋更省的建模——门级模拟撑不起那么大。
- 计爻器 之跳转口在雏机中接地（阴列），尚未接上真正的跳转来源——那是 M3 控制单元的事。
- **文王卦序 ↔ 先天二进制映射表**仍未补（M3 前置，见 M0 遗留）。

---

## M1 · 四象算术 —— 已成

**成于**：2026-08-30

### 所成之物

| 文件 | 内容 |
|---|---|
| `算术.go` | 半加器、全加器、行波进位加法器、加一器、求补器、多择门、**二十四位算爻器** |
| `门.go`（增） | `恒源`（电源之轨）、`恒阴` / `恒阳`、`私列`、`列线` |
| `演示.go`（增） | `演示算爻`：八操演算，各配一经卦 |
| `算术_test.go` | 八项测试 |

**算爻器之八操**，以三爻（一卦）择之——此即「以卦为操作码」之雏形：

| 值 | 卦 | 运算 | | 值 | 卦 | 运算 |
|---|---|---|---|---|---|---|
| 0 | 坤 ☷ | 加 | | 4 | 震 ☳ | 逐位异或 |
| 1 | 艮 ☶ | 减 | | 5 | 离 ☲ | 左移一爻，低位补阴 |
| 2 | 坎 ☵ | 逐位与 | | 6 | 兑 ☱ | 右移一爻，高位补阴 |
| 3 | 巽 ☴ | 逐位或 | | 7 | 乾 ☰ | 甲 小于 乙（有符号）则出一 |

四志：**进位**（减法时为「无借位」）、**零**、**负**、**溢出**。

### 验收

- [x] 半加器、全加器穷举
- [x] 四位加法器穷举 256 组；**八位加法器穷举 65 536 组**
- [x] 八位求补器穷举 256 组，并验「零之补仍为零」
- [x] 三位选择八入多择门穷举 2 048 组
- [x] **四位算爻器八操穷举 2 048 组**，与 Go 原生对拍
- [x] **二十四位算爻器对拍**：边界 800 例（十个数交叉 × 八操）+ 随机 2 000 例
- [x] 万门出于一：二十四位算爻器 **2 472 门 / 2 524 线**，除与非门与恒源外别无他物

### 修去的三处 bug（皆值得一记）

1. **`a - b & 掩` 实为 `a - (b & 掩)`** —— Go 中 `&` 比 `-` 绑得紧。
2. **`v & 1 << (位-1)` 实为 `(v & 1) << (位-1)`** —— `&` 与 `<<` 同级而左结合，
   正数遂被误判为负。二者皆因省括号而起，已立戒于 `典/02-实现约束.md` 第六条。
3. **调定的步数是跨调用累加的** —— 反复驱同一网表久了，纵是完好之电路，
   累计逾百万步后也会误报「振荡」。已改为「本次调定之步」与「累计之步」两数分计。

### 一处设计取舍：恒源不入「万门出于一」之验

移位补零与减法的进位输入都需要恒定电平。取巧的 `NAND(a, ¬a)` 能造出恒阳，
但在事件驱动之下，a 变而 ¬a 未及变，会先吐一个假值再复真——**那是一次真实的毛刺**。
故另立 `恒源` 为电源之轨：它有出无入，本非门也，犹天地之有阴阳。
「万门出于一」之验遂改为：凡**受入而算**者，皆出于与非门；恒源不与焉。

### 已知之限

- 移位只移一爻，非桶形移位器（可变位数之移位留待 ISA 定稿时再议）。
- 二十四位算爻器为组合电路，尚无寄存器与时钟——**时序未启，故尚不能称为「机」**。

